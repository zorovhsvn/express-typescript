### Create project expressjs + mysql + sequelize (Typescript)
## Install module
```
npm i --save-dev @types/compression @types/dotenv @types/express @types/express-session @types/fs-extra @types/morgan @types/node @types/shelljs @types/validator npm-run-all fs-extra rimraf shelljs
```
```
npm i -g nodemon pm2 ts-node tslint typescript
```
```
npm i body-parser compression dotenv ejs express express-session morgan mysql2 sequelize typescript
```
## Start
```
npm run dev
```
```
npm start
```